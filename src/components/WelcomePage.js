import { Heading } from "@chakra-ui/react"

export const WelcomePage = () => {
    return    (
        <Heading marginTop={20} marginLeft={20} fontSize={25}> Hello, sign in or register if you want to use phonebook</Heading>
    )
}